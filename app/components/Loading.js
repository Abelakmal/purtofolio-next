'use client';
export const Loading = () => {
  return (
    <section className="bg-black h-screen relative w-screen flex justify-center items-center" style={{ background: 'linear-gradient(to right, #000428, #072f52)' }}>
      <h1>LOADING...</h1>
    </section>
  );
};
